#include <stdio.h>

int main() {
    char a;
    scanf("%c", &a);

    if (a = "a" || a = "i" || a = "o") {
        printf("Vowel");
    }
    else {
        printf("Constant");
    }

    return 0;
}
