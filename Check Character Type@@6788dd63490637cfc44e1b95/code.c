#include <stdio.h>

int main() {
    char a;
    scanf("%c", &a);

    if (a = "a,i,o,u,e") {
        printf("Vowel");
    }
    else {
        printf("Constant");
    }

    return 0;
}
